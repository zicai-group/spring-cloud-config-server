package io.gitee.zicai.test.console

/** errs the given message to the standard output stream. */

fun err(message: Any?) {
    System.err.print(message)
}

/** errs the given message to the standard output stream. */

fun err(message: Int) {
    System.err.print(message)
}

/** errs the given message to the standard output stream. */

fun err(message: Long) {
    System.err.print(message)
}

/** errs the given message to the standard output stream. */

fun err(message: Byte) {
    System.err.print(message)
}

/** errs the given message to the standard output stream. */

fun err(message: Short) {
    System.err.print(message)
}

/** errs the given message to the standard output stream. */

fun err(message: Char) {
    System.err.print(message)
}

/** errs the given message to the standard output stream. */

fun err(message: Boolean) {
    System.err.print(message)
}

/** errs the given message to the standard output stream. */

fun err(message: Float) {
    System.err.print(message)
}

/** errs the given message to the standard output stream. */

fun err(message: Double) {
    System.err.print(message)
}

/** errs the given message to the standard output stream. */

fun err(message: CharArray) {
    System.err.print(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: Any?) {
    System.err.println(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: Int) {
    System.err.println(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: Long) {
    System.err.println(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: Byte) {
    System.err.println(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: Short) {
    System.err.println(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: Char) {
    System.err.println(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: Boolean) {
    System.err.println(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: Float) {
    System.err.println(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: Double) {
    System.err.println(message)
}

/** errs the given message and newline to the standard output stream. */

fun errln(message: CharArray) {
    System.err.println(message)
}

/** errs a newline to the standard output stream. */

fun errln() {
    System.err.println()
}