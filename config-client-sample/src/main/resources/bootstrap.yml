
server:
  port: 8899

spring:
  application:
    name: config-sample

  cloud:
    consul:
      enabled: ${spring.cloud.config.discovery.enabled}
      host: 192.168.0.34
      port: 8500
      discovery:
        enabled: true
        register: false
    config:
      name: ${spring.application.name}
      profile: test
      fail-fast: true
#      uri: http://localhost:8888
      discovery:
        enabled: ${config.sync.discovery}
        service-id: zicai-config-server

config:
  sync:
    discovery: false
#    syncUri: http://${spring.cloud.config.discovery.service-id}/published/last
    syncUri: http://localhost:8888/published/last
    refreshUri: http://localhost:${server.port}/refresh

management:
  security:
    enabled: false

